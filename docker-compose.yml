version: '3.8'

services:
  zoa-flow-erp:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8080
      - GOOGLE_APPLICATION_CREDENTIALS=/app/service-account.json
